{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the local ChatChat Council window.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
